import 'package:flutter/material.dart';

class allChat extends StatelessWidget {
  String? isMessagesByMe;
   allChat({Key? key, this.isMessagesByMe}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
      ),
    );
  }
}
